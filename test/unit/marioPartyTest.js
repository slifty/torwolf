// TODO: remove this test when we have real tests

var should = require('chai').should()

describe('Put some context here', function() {
  describe('Put moar context here (optional)', function () {
    it('Should be true', function () {
      "Mario Party is awesome".should.not.be.false
    });
  });
});